#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../execsql/bin/execsql" "$@"
  ret=$?
else 
  node  "$basedir/../execsql/bin/execsql" "$@"
  ret=$?
fi
exit $ret
